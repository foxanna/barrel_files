/// Annotate top-level dart code element
/// with `@includeInBarrelFile` or `@IncludeInBarrelFile()`
/// for it to appear in the generated barrel file
const includeInBarrelFile = IncludeInBarrelFile();

/// Annotate top-level dart code element
/// with `@includeInBarrelFile` or `@IncludeInBarrelFile()`
/// for it to appear in the generated barrel file
class IncludeInBarrelFile {
  const IncludeInBarrelFile();
}
